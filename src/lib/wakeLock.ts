/**
 * Variante NATIVA (stub): en F5 se usará expo-keep-awake.
 * En web se usa wakeLock.web.ts (Screen Wake Lock API).
 */
export function setKeepAwake(_active: boolean): void {
  // no-op en nativo por ahora
}
